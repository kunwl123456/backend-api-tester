# Backend API Tester 配置示例
# 复制此文件为 .env，填入真实值后运行

# 后端服务地址（不含路径，不含尾部斜杠）
BACKEND_BASE_URL=http://localhost:31127

# JWT Token（可在启动后通过 UI 登录自动获取，无需预先填写）
BACKEND_JWT_TOKEN=

# 默认用户名（在对话测试中作为 user_name 使用）
BACKEND_USERNAME=player_001

# 本工具监听端口（默认 8080）
PORT=8080
